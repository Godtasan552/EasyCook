name: easycook
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.5
  http: ^1.5.0
  sqflite: ^2.2.8+4
  path: ^1.8.3
  share_plus: ^11.0.0
  url_launcher: ^6.1.10
  intl: ^0.20.2
  cached_network_image: ^3.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/reset.png
